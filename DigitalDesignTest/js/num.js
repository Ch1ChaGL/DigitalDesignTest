'use strict';

// const numInput = document.querySelector('#num__input');

// numInput.onkeydown = event => {
//   return event.key !== '-' && event.key !== ',' && event.key !== '.';
// };

// numInput.addEventListener('keypress', event => {

//   const count = parseInt(event.target.value);

//   const priceElement = document.querySelector('.popup-form__price');   
//   const priceText = priceElement.innerHTML;
//   const price = parseInt(priceText.slice(0, priceText.length - 1));

//   priceElement.innerHTML = `${price*count} ₽`;

// });
